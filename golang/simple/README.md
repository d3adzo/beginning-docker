# Interacting with a Go project using Docker

## Usage
```sh
docker build -t go-hello . # builds the Go docker image
docker run go-hello # prints hello world!
docker run go-hello ritlug # prints hello ritlug
```